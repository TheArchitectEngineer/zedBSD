--- a/server/render_client.c
+++ b/server/render_client.c
@@ -223,7 +223,7 @@
                             const union render_client_op_request *req)
 {
    if (req->init.zedbsd_magic != VIRGL_VENUS_ZEDBSD_MAGIC ||
-       req->init.zedbsd_flags != VIRGL_VENUS_ZEDBSD_OPAQUE_SHARING)
+       req->init.zedbsd_flags != VIRGL_VENUS_ZEDBSD_FEATURES)
       return false;
 
    client->init_flags = req->init.flags;
@@ -234,7 +234,7 @@
 
    const struct render_client_op_init_reply reply = {
       .zedbsd_magic = VIRGL_VENUS_ZEDBSD_MAGIC,
-      .zedbsd_flags = VIRGL_VENUS_ZEDBSD_OPAQUE_SHARING,
+      .zedbsd_flags = VIRGL_VENUS_ZEDBSD_FEATURES,
    };
    return render_socket_send_reply(&client->socket, &reply, sizeof(reply));
 }
--- a/src/proxy/proxy_client.c
+++ b/src/proxy/proxy_client.c
@@ -87,7 +87,7 @@
       .header.op = RENDER_CLIENT_OP_INIT,
       .flags = flags,
       .zedbsd_magic = VIRGL_VENUS_ZEDBSD_MAGIC,
-      .zedbsd_flags = VIRGL_VENUS_ZEDBSD_OPAQUE_SHARING,
+      .zedbsd_flags = VIRGL_VENUS_ZEDBSD_FEATURES,
    };
    if (!proxy_socket_send_request(&client->socket, &req, sizeof(req)))
       return false;
@@ -96,10 +96,10 @@
    if (!proxy_socket_receive_reply(&client->socket, &reply, sizeof(reply)))
       return false;
    if (reply.zedbsd_magic != VIRGL_VENUS_ZEDBSD_MAGIC ||
-       reply.zedbsd_flags != VIRGL_VENUS_ZEDBSD_OPAQUE_SHARING)
+       reply.zedbsd_flags != VIRGL_VENUS_ZEDBSD_FEATURES)
       return false;
 
-   client->zedbsd_opaque_sharing = true;
+   client->zedbsd_features = reply.zedbsd_flags;
    return true;
 }
 
--- a/src/proxy/proxy_client.h
+++ b/src/proxy/proxy_client.h
@@ -12,7 +12,7 @@
    struct proxy_socket socket;
 
    /* Set only after the paired server acknowledges the exact opaque-sharing contract. */
-   bool zedbsd_opaque_sharing;
+   uint32_t zedbsd_features;
 };
 
 struct proxy_client *
--- a/src/proxy/proxy_context.c
+++ b/src/proxy/proxy_context.c
@@ -106,7 +106,6 @@
                                             uint32_t cur_seqno)
 {
    struct proxy_timeline *timeline = &ctx->timelines[ring_idx];
-   bool force_retire_all = false;
 
    /* check if the socket has been disconnected (i.e., the other end has
     * crashed) if no progress is made after a while
@@ -117,15 +116,15 @@
           proxy_socket_is_connected(&ctx->socket))
          return false;
 
-      /* socket has been disconnected */
-      force_retire_all = true;
+      /* A disconnected renderer has no authority to invent successful GPU completion. */
+      return false;
    }
 
    timeline->cur_seqno = cur_seqno;
    timeline->cur_seqno_stall_count = 0;
 
    list_for_each_entry_safe (struct proxy_fence, fence, &timeline->fences, head) {
-      if (!proxy_fence_is_signaled(fence, timeline->cur_seqno) && !force_retire_all)
+      if (!proxy_fence_is_signaled(fence, timeline->cur_seqno))
          return false;
 
       ctx->base.fence_retire(&ctx->base, ring_idx, fence->fence_id);
@@ -188,6 +187,10 @@
       }
 
       proxy_context_retire_fences_internal(ctx);
+
+      /* HUP is permanently readable; leave pending fences for the guest watchdog. */
+      if (poll_fds[1].revents & (POLLHUP | POLLERR | POLLNVAL))
+         break;
    }
 
    return 0;
--- a/src/proxy/proxy_renderer.c
+++ b/src/proxy/proxy_renderer.c
@@ -64,9 +64,10 @@
       static_assert(sizeof(*c) == 168, "zedBSD suffix must follow the 160-byte capset");
       static_assert(offsetof(struct virgl_renderer_capset_venus, zedbsd_magic) == 160,
                     "zedBSD capability magic offset");
-      if (c && proxy_renderer.client && proxy_renderer.client->zedbsd_opaque_sharing) {
+      if (c && proxy_renderer.client &&
+          proxy_renderer.client->zedbsd_features == VIRGL_VENUS_ZEDBSD_FEATURES) {
          c->zedbsd_magic = VIRGL_VENUS_ZEDBSD_MAGIC;
-         c->zedbsd_flags = VIRGL_VENUS_ZEDBSD_OPAQUE_SHARING;
+         c->zedbsd_flags = proxy_renderer.client->zedbsd_features;
       }
       return size;
    }
--- a/src/venus/vkr_context.c
+++ b/src/venus/vkr_context.c
@@ -136,6 +136,10 @@
       return false;
    }
 
+   /* A later sequence must never retire earlier work whose success is unknown. */
+   if (atomic_load_explicit(&ctx->queue_sync_failed, memory_order_acquire))
+      return false;
+
    /* always merge fences */
    assert(!(flags & ~VIRGL_RENDERER_FENCE_FLAG_MERGEABLE));
    flags = VIRGL_RENDERER_FENCE_FLAG_MERGEABLE;
@@ -667,6 +671,7 @@
 
    ctx->ctx_id = ctx_id;
    ctx->retire_fence = cb;
+   atomic_init(&ctx->queue_sync_failed, false);
    ctx->debug_name = malloc(debug_len + 1);
    if (!ctx->debug_name)
       goto err_debug_name;
--- a/src/venus/vkr_context.h
+++ b/src/venus/vkr_context.h
@@ -96,6 +96,9 @@
 
    struct vkr_queue *sync_queues[64];
 
+   /* A failed queue marker forbids every later success watermark in this context. */
+   atomic_bool queue_sync_failed;
+
    struct vkr_instance *instance;
    char *instance_name;
 
--- a/src/venus/vkr_device.c
+++ b/src/venus/vkr_device.c
@@ -315,6 +315,10 @@
          vkr_log("vkDeviceWaitIdle(%p) failed(%d)", dev, (int32_t)result);
    }
 
+   /* End borrowed submission-fence waits before destroying device objects. */
+   list_for_each_entry_safe (struct vkr_queue, queue, &dev->queues, base.track_head)
+      vkr_queue_destroy(ctx, queue);
+
    mtx_destroy(&dev->object_mutex);
 
    if (!list_is_empty(&dev->objects)) {
@@ -322,11 +326,8 @@
          vkr_device_object_destroy(ctx, dev, obj);
    }
 
-   list_for_each_entry_safe (struct vkr_queue, queue, &dev->queues, base.track_head)
-      vkr_queue_destroy(ctx, queue);
-
    list_for_each_entry_safe (struct vkr_queue_sync, sync, &dev->free_syncs, head) {
-      vk->DestroyFence(dev->base.handle.device, sync->fence, NULL);
+      /* Queue records never own or destroy their borrowed native fence. */
       free(sync);
    }
 
--- a/src/venus/vkr_queue.c
+++ b/src/venus/vkr_queue.c
@@ -11,103 +11,119 @@
 #include "vkr_physical_device.h"
 #include "vkr_queue_gen.h"
 
+/* Allocates a queue marker record without creating or resetting a native fence. */
 static struct vkr_queue_sync *
-vkr_device_alloc_queue_sync(struct vkr_device *dev,
-                            uint32_t fence_flags,
-                            uint32_t ring_idx,
-                            uint64_t fence_id)
-{
-   struct vn_device_proc_table *vk = &dev->proc_table;
+vkr_device_alloc_queue_sync(
+   struct vkr_device *dev,
+   uint32_t fence_flags,
+   uint32_t ring_idx,
+   uint64_t fence_id)
+{
    struct vkr_queue_sync *sync;
 
+   /* Reuses only bookkeeping whose previous host fence access has ended. */
    mtx_lock(&dev->free_sync_mutex);
+
    if (list_is_empty(&dev->free_syncs)) {
       mtx_unlock(&dev->free_sync_mutex);
-
       sync = malloc(sizeof(*sync));
       if (!sync)
          return NULL;
-
-      const VkExportFenceCreateInfo export_info = {
-         .sType = VK_STRUCTURE_TYPE_EXPORT_FENCE_CREATE_INFO,
-         .handleTypes = VK_EXTERNAL_FENCE_HANDLE_TYPE_SYNC_FD_BIT,
-      };
-      const struct VkFenceCreateInfo create_info = {
-         .sType = VK_STRUCTURE_TYPE_FENCE_CREATE_INFO,
-         .pNext = dev->physical_device->KHR_external_fence_fd ? &export_info : NULL,
-      };
-      VkResult result =
-         vk->CreateFence(dev->base.handle.device, &create_info, NULL, &sync->fence);
-      if (result != VK_SUCCESS) {
-         free(sync);
-         vkr_log("failed to create sync fence for fence_id %" PRIu64, fence_id);
-         return NULL;
-      }
    } else {
       sync = LIST_ENTRY(struct vkr_queue_sync, dev->free_syncs.next, head);
       list_del(&sync->head);
       mtx_unlock(&dev->free_sync_mutex);
-
-      vk->ResetFences(dev->base.handle.device, 1, &sync->fence);
-   }
-
-   sync->device_lost = false;
+   }
+
+   /* The successful queue submission supplies the borrowed fence later. */
+   sync->fence = VK_NULL_HANDLE;
    sync->flags = fence_flags;
    sync->ring_idx = ring_idx;
    sync->fence_id = fence_id;
 
+   /* Succeeded: this record owns no native Vulkan object. */
    return sync;
 }
 
-static void
-vkr_device_free_queue_sync(struct vkr_device *dev, struct vkr_queue_sync *sync)
-{
+/* Recycles a completed record after relinquishing its borrowed fence identity. */
+static void
+vkr_device_free_queue_sync(
+   struct vkr_device *dev,
+   struct vkr_queue_sync *sync)
+{
+   /* The guest owns destruction and reset of the real submission fence. */
+   sync->fence = VK_NULL_HANDLE;
    mtx_lock(&dev->free_sync_mutex);
+
    list_addtail(&sync->head, &dev->free_syncs);
+
    mtx_unlock(&dev->free_sync_mutex);
 }
 
+/* Publishes verified success only while no earlier queue completion is uncertain. */
 static inline void
-vkr_queue_sync_retire(struct vkr_queue *queue, struct vkr_queue_sync *sync)
-{
-   TRACE_FUNC();
-   queue->context->retire_fence(queue->context->ctx_id, sync->ring_idx, sync->fence_id);
+vkr_queue_sync_retire(
+   struct vkr_queue *queue,
+   struct vkr_queue_sync *sync)
+{
+   bool failed;
+
+   /* A later watermark must not conceal a failed earlier submission. */
+   failed = atomic_load_explicit(&queue->context->queue_sync_failed, memory_order_acquire);
+   if (!failed)
+      queue->context->retire_fence(queue->context->ctx_id, sync->ring_idx, sync->fence_id);
+
+   /* No native fence access is allowed after publishing the successful callback. */
    vkr_device_free_queue_sync(queue->device, sync);
 }
 
+/*
+ * Binds a marker to the actual latest successful submission's native fence.
+ */
 bool
-vkr_queue_sync_submit(struct vkr_queue *queue,
-                      uint32_t flags,
-                      uint32_t ring_idx,
-                      uint64_t fence_id)
-{
-   TRACE_FUNC();
-   struct vkr_device *dev = queue->device;
-   struct vn_device_proc_table *vk = &dev->proc_table;
-
-   struct vkr_queue_sync *sync =
-      vkr_device_alloc_queue_sync(dev, flags, ring_idx, fence_id);
-   if (!sync)
+vkr_queue_sync_submit(
+   struct vkr_queue *queue,
+   uint32_t flags,
+   uint32_t ring_idx,
+   uint64_t fence_id)
+{
+   struct vkr_device *dev;
+   struct vkr_queue_sync *sync;
+   bool failed;
+
+   /* Reserve host bookkeeping before consuming the submission-to-marker handoff. */
+   dev = queue->device;
+   sync = vkr_device_alloc_queue_sync(dev, flags, ring_idx, fence_id);
+   if (!sync) {
+      atomic_store_explicit(&queue->context->queue_sync_failed, true, memory_order_release);
       return false;
-
+   }
+
+   /* Queue serialization associates this marker with exactly one native submission. */
    mtx_lock(&queue->vk_mutex);
-   VkResult result = vk->QueueSubmit(queue->base.handle.queue, 0, NULL, sync->fence);
+
+   failed = atomic_load_explicit(&queue->context->queue_sync_failed, memory_order_acquire);
+   sync->fence = queue->submission_fence;
+   queue->submission_fence = VK_NULL_HANDLE;
+
    mtx_unlock(&queue->vk_mutex);
 
-   if (result == VK_ERROR_DEVICE_LOST) {
-      sync->device_lost = true;
-      vkr_log("sync submit hit device lost for fence_id %" PRIu64, fence_id);
-   } else if (result != VK_SUCCESS) {
+   /* No empty submit can substitute for an absent or rejected sparse/command submission. */
+   if (failed || sync->fence == VK_NULL_HANDLE) {
+      atomic_store_explicit(&queue->context->queue_sync_failed, true, memory_order_release);
       vkr_device_free_queue_sync(dev, sync);
-      vkr_log("sync submit failed (vk ret %d) for fence_id %" PRIu64, result, fence_id);
       return false;
    }
 
+   /* The guest retains this native fence until the corresponding kernel job terminates. */
    mtx_lock(&queue->sync_thread.mutex);
+
    list_addtail(&sync->head, &queue->sync_thread.syncs);
    cnd_signal(&queue->sync_thread.cond);
+
    mtx_unlock(&queue->sync_thread.mutex);
 
+   /* Succeeded: only a successful native wait can publish this marker. */
    return true;
 }
 
@@ -122,8 +138,14 @@
 
    thrd_join(queue->sync_thread.thread, NULL);
 
-   list_for_each_entry_safe (struct vkr_queue_sync, sync, &queue->sync_thread.syncs, head)
-      vkr_queue_sync_retire(queue, sync);
+   /* Unverified teardown must never advance the guest's successful watermark. */
+   if (!list_is_empty(&queue->sync_thread.syncs))
+      atomic_store_explicit(&queue->context->queue_sync_failed, true, memory_order_release);
+
+   list_for_each_entry_safe (struct vkr_queue_sync, sync, &queue->sync_thread.syncs, head) {
+      list_del(&sync->head);
+      vkr_device_free_queue_sync(queue->device, sync);
+   }
 
    mtx_destroy(&queue->sync_thread.mutex);
    cnd_destroy(&queue->sync_thread.cond);
@@ -174,7 +196,7 @@
       mtx_unlock(&queue->sync_thread.mutex);
 
       VkResult result;
-      if (sync->device_lost) {
+      if (atomic_load_explicit(&ctx->queue_sync_failed, memory_order_acquire)) {
          result = VK_ERROR_DEVICE_LOST;
       } else {
          result = vk->WaitForFences(dev->base.handle.device, 1, &sync->fence, true,
@@ -187,6 +209,14 @@
          continue;
 
       list_del(&sync->head);
+
+      /* Failure stays sticky and unacknowledged until the guest watchdog reports error. */
+      if (result != VK_SUCCESS) {
+         atomic_store_explicit(&ctx->queue_sync_failed, true, memory_order_release);
+         vkr_log("strict queue wait failed (%d), fence_id %" PRIu64, result, sync->fence_id);
+         vkr_device_free_queue_sync(dev, sync);
+         continue;
+      }
 
       vkr_queue_sync_retire(queue, sync);
    }
@@ -243,6 +273,7 @@
    queue->flags = flags;
    queue->family = family;
    queue->index = index;
+   queue->submission_fence = VK_NULL_HANDLE;
 
    if (mtx_init(&queue->vk_mutex, mtx_plain)) {
       free(queue);
@@ -376,8 +407,16 @@
    vn_replace_vkQueueSubmit_args_handle(args);
 
    mtx_lock(&queue->vk_mutex);
+
+   /* A rejected operation must not leave an older native fence available to a marker. */
+   queue->submission_fence = VK_NULL_HANDLE;
    args->ret =
       vk->QueueSubmit(args->queue, args->submitCount, args->pSubmits, args->fence);
+   if (args->ret == VK_SUCCESS)
+      queue->submission_fence = args->fence;
+   else if (args->ret == VK_ERROR_DEVICE_LOST)
+      atomic_store_explicit(&queue->context->queue_sync_failed, true, memory_order_release);
+
    mtx_unlock(&queue->vk_mutex);
 }
 
@@ -392,8 +431,16 @@
    vn_replace_vkQueueBindSparse_args_handle(args);
 
    mtx_lock(&queue->vk_mutex);
+
+   /* A rejected operation must not leave an older native fence available to a marker. */
+   queue->submission_fence = VK_NULL_HANDLE;
    args->ret =
       vk->QueueBindSparse(args->queue, args->bindInfoCount, args->pBindInfo, args->fence);
+   if (args->ret == VK_SUCCESS)
+      queue->submission_fence = args->fence;
+   else if (args->ret == VK_ERROR_DEVICE_LOST)
+      atomic_store_explicit(&queue->context->queue_sync_failed, true, memory_order_release);
+
    mtx_unlock(&queue->vk_mutex);
 }
 
@@ -417,8 +464,16 @@
    vn_replace_vkQueueSubmit2_args_handle(args);
 
    mtx_lock(&queue->vk_mutex);
+
+   /* A rejected operation must not leave an older native fence available to a marker. */
+   queue->submission_fence = VK_NULL_HANDLE;
    args->ret =
       vk->QueueSubmit2(args->queue, args->submitCount, args->pSubmits, args->fence);
+   if (args->ret == VK_SUCCESS)
+      queue->submission_fence = args->fence;
+   else if (args->ret == VK_ERROR_DEVICE_LOST)
+      atomic_store_explicit(&queue->context->queue_sync_failed, true, memory_order_release);
+
    mtx_unlock(&queue->vk_mutex);
 }
 
--- a/src/venus/vkr_queue.h
+++ b/src/venus/vkr_queue.h
@@ -9,8 +9,8 @@
 #include "vkr_common.h"
 
 struct vkr_queue_sync {
+   /* Borrows the real submission fence until the final host wait finishes. */
    VkFence fence;
-   bool device_lost;
 
    uint32_t flags;
    uint32_t ring_idx;
@@ -36,6 +36,9 @@
     * thread and ring thread.
     */
    mtx_t vk_mutex;
+
+   /* The next marker consumes the latest successful submission fence. */
+   VkFence submission_fence;
 
    /* Submitted fences are added to sync_thread.syncs first. With required
     * VKR_RENDERER_THREAD_SYNC and VKR_RENDERER_ASYNC_FENCE_CB in render server, the sync
--- a/src/venus_hw.h
+++ b/src/venus_hw.h
@@ -29,6 +29,8 @@
 /* Isolated zedBSD renderer protocol: both proxy and server must acknowledge it. */
 #define VIRGL_VENUS_ZEDBSD_MAGIC 0x5a424453U
 #define VIRGL_VENUS_ZEDBSD_OPAQUE_SHARING 1U
+#define VIRGL_VENUS_ZEDBSD_STRICT_QUEUE 2U
+#define VIRGL_VENUS_ZEDBSD_FEATURES 3U
 
 struct virgl_renderer_capset_venus {
    uint32_t wire_format_version;
